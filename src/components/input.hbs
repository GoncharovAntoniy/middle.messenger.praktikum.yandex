<div class="inputContainer">
  <input
    name="{{name}}"
    id="{{inputId}}"
    class="{{classInput}}"
    type="{{typeInput}}"
    required
  />
  <label class="labelInput" for="{{inputId}}">{{placeholderInput}}</label>
</div>
